name: CD Deploy (Self-Hosted)

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-fileyo
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: [self-hosted, macOS]
    steps:
      - name: Deploy app on production machine
        run: /Users/hanbim/Development/fileyo-workspace/deploy/production/scripts/deploy-from-push.sh fileyo
